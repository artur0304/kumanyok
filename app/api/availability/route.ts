import {db,fail} from '@/lib/server';
export async function GET(r:Request){try{const date=new URL(r.url).searchParams.get('date');if(!date||!/^\d{4}-\d{2}-\d{2}$/.test(date))return Response.json({error:'Некоректна дата'},{status:400});const data=await db().prepare("SELECT resource,start,end FROM bookings WHERE date=? AND status != 'cancelled'").bind(date).all();return Response.json({slots:data.results},{headers:{'Cache-Control':'no-store'}})}catch(e){return fail(e)}}
