import {z} from 'zod';
import {db,catalog,fail,sameOrigin} from '@/lib/server';
import {totalPrice} from '@/lib/catalog';
const booking=z.object({resource:z.string().max(30),date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).refine(v=>!isNaN(Date.parse(v))&&new Date(v).toISOString().slice(0,10)===v),start:z.number().int().min(0).max(23),end:z.number().int().min(1).max(24),guests:z.number().int().min(1).max(100),deposit:z.number().int().min(500),name:z.string().trim().min(1).max(80),phone:z.string().regex(/^\+?[\d\s()-]{10,22}$/)});
export async function POST(req:Request){try{if(!sameOrigin(req))return Response.json({error:'Недозволений запит'},{status:403});const parsed=booking.safeParse(await req.json());if(!parsed.success)return Response.json({error:'Перевірте дату, час, кількість гостей та контакти.'},{status:400});const b=parsed.data;const {resources,settings}=await catalog();const r=resources.find(r=>r.id===b.resource);const now=new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Kyiv'}).format(new Date());const max=new Date();max.setFullYear(max.getFullYear()+1);if(!r||b.date<now||b.date>max.toISOString().slice(0,10)||b.start<settings.open||b.end>settings.close||b.end-b.start<(r.kind==='sauna'?3:1))return Response.json({error:'Перевірте дату та час. Баня — щонайменше 3 години.'},{status:400});const total=totalPrice(r,b.date,b.start,b.end,b.guests,settings);if(b.deposit>total)return Response.json({error:'Передоплата не може перевищувати повну суму.'},{status:400});const id=crypto.randomUUID();const start=r.kind==='gazebo'?0:b.start,end=r.kind==='gazebo'?24:b.end;const result=await db().prepare("INSERT INTO bookings (id,resource,date,start,end,guests,name,phone,total,deposit,status,created,requested_start,requested_end) SELECT ?,?,?,?,?,?,?,?,?,?,'pending',?,?,? WHERE NOT EXISTS (SELECT 1 FROM bookings WHERE resource=? AND date=? AND status!='cancelled' AND start<? AND end>?)").bind(id,r.id,b.date,start,end,b.guests,b.name,b.phone,total,b.deposit,new Date().toISOString(),b.start,b.end,r.id,b.date,end,start).run();if(result.meta.changes!==1)return Response.json({error:'Цей час уже зайнято. Оберіть інший час або день.'},{status:409});return Response.json({id,total,deposit:b.deposit},{status:201});}catch(e){return fail(e)}}
